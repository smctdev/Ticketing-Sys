services:
  ticketing-server:
    build:
      context: ./
      dockerfile: Dockerfile
    image: ticketing-server
    container_name: ticketing-server
    restart: unless-stopped
    environment:
      DB_CONNECTION: mysql
      DB_HOST: 172.17.0.1
      DB_PORT: 3306
      DB_DATABASE: ticketing_netsuite_v2
      DB_USERNAME: smct
      DB_PASSWORD: smct
    volumes:
      - ./docker-compose/nginx/default.conf:/etc/nginx/nginx.conf
      - ./:/var/www
      - /mnt/nvme0n1/ticketing-v2-uploads:/var/www/storage/app/public
      - ./php.ini:/usr/local/etc/php/php.ini
      - /home/webserver/systems/Ticketing-V2/Ticketing-Sys/server/.env:/var/www/.env
    networks:
      - ticketing-server

  ticketing-server-nginx:
    image: nginx:alpine
    container_name: ticketing-server-nginx
    restart: unless-stopped
    ports:
      - 7040:7040
    volumes:
      - ./docker-compose/nginx/default.conf:/etc/nginx/nginx.conf
      - ./:/var/www
      - /mnt/nvme0n1/ticketing-v2-uploads:/var/www/storage/app/public
      - ./php.ini:/usr/local/etc/php/php.ini
      - /home/webserver/systems/Ticketing-V2/Ticketing-Sys/server/.env:/var/www/.env

    networks:
      - ticketing-server

  ticketing-server-reverb:
    build:
      context: ./
      dockerfile: Dockerfile-reverb
    ports:
    - "8084:8084" # expose Reverb server
    environment:
    - APP_URL=${APP_URL}
    - LARAVEL_REVERB_APP_KEY=${REVERB_APP_KEY}
    - LARAVEL_REVERB_APP_SECRET=${REVERB_APP_SECRET}
    depends_on:
    - ticketing-server
    restart: unless-stopped
    container_name: ticketing-server-reverb

    networks:
      - ticketing-server

  ticketing-v2-queue-worker:
    image: ticketing-server
    container_name: ticketing-v2-queue-worker
    working_dir: /var/www/
    depends_on:
      - ticketing-server
    command: php artisan queue:work redis --tries=3
    volumes:
      - ./storage:/var/www/storage
      - ./bootstrap/cache:/var/www/bootstrap/cache
      - ./:/var/www
    networks:
      - ticketing-server
    restart: unless-stopped

networks:
  ticketing-server:
    driver: bridge
